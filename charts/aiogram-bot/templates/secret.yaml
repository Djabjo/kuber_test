apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.bot.name | default (include "my-bot.fullname" .) }}
  labels:
    {{- include "my-bot.labels" . | nindent 4 }}
type: Opaque
data:
  telegram-token: {{ .Values.bot.existingSecret | b64enc }}